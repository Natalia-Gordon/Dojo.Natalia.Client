version: '3.4'

services:
  nataliadojowebsite:
    image: nataliadojowebsite
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 8080:80
